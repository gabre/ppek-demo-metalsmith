{% extends "index_main.njk" %}
{% block pageMain %}
  <div class="content">
    <h2 id="the-foundation" class="content-subhead">
      {{ tag }} könyvei
    </h2>
    <div class="table-responsive">
      <ul>
        {% for authorbook in pagination.files %}
          <li>
            <a href="{{rootRelativePath}}/{{authorbook.ownRelativePathFromRoot}}">{{ authorbook.title }}</a> ({{ authorbook.pubdate }})
          </li>
          {# <td><a href="{{rootRelativePath}}/{{book.ownRelativePathFromRoot}}">{{ book.title }}</a></td> #}
        {% endfor %}
      </ul>
    </div>
  </div>
{% endblock pageMain %}